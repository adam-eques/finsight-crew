# Comparing tickers

```bash
finsight compare AAPL MSFT NVDA --metric trailingPE
```

Fetches the chosen metric for each ticker and prints them ranked. The
`compare` helpers are also importable for building comparison tables.
