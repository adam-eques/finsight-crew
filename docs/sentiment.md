# Sentiment

`finsight.tools.sentiment.score` returns a tone score in [-1, 1] using a
small, transparent lexicon with optional negation handling. It is wired
to the researcher agent so headlines can be tone-tagged.
