# Evaluation

`finsight.evaluation.completeness` gives a 0..1 score for how fully a
report is populated (summary, findings, metrics, risks, citations). Use
`scripts/score_report.py` to score a saved JSON brief in CI.
