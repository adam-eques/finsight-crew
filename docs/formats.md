# Output formats

`finsight research --format {md,json,html}` selects the brief format.

- `md` — human-readable markdown (default)
- `json` — the structured `Report` for downstream tooling
- `html` — a standalone page
